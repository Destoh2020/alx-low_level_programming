#include "main.h"
/**
 * _abs - Computes absolute values of number
 *
 * @n: The number to be checked
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
